# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

Add a changeset for any user-facing change:

```bash
pnpm changeset
```

Pick the bump (major/minor/patch) and write a short summary. The release workflow
opens a version PR that, when merged, publishes to npm with provenance.
